# Critical: Install a2a-sdk FIRST (provides 'a2a' module)
a2a-sdk>=0.2.8

# Then install any-agent with BOTH a2a and openai extras
any-agent[a2a,openai]>=0.21.0

# App dependencies
python-dotenv
pydantic
gradio>=4.0.0
nest-asyncio
aiohttp
typing-extensions

# Note: any-agent[a2a,openai] will pull in all needed dependencies including:
# - grpcio, protobuf, httpx (from a2a)
# - openai, litellm (from openai)
# We list a2a-sdk explicitly first to ensure the 'a2a' module is available
